{
  "scripts": {
    "kdanseapi": "browserify index.js --standalone kdanseapi -o kdanse-api.js",
    "test": "jest --coverage --collectCoverageFrom=index.js",
    "testEnvironment": "jsdom"
  },
  "dependencies": {
    "blakejs": "^1.2.1",
    "browserify": "^17.0.0",
    "jest": "^29.5.0"
  },
  "devDependencies": {
    "jsdom": "^21.1.0",
    "testEnvironment": "jsdom"
  }
}
